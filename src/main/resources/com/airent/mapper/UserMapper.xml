<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.airent.mapper.UserMapper">
    <insert id="createUser" keyProperty="id" useGeneratedKeys="true">
       INSERT INTO sys_user (phone, name, password, trustRate, registered)
       VALUES (#{phone}, #{name}, #{password}, #{trustRate}, #{registered})
    </insert>

    <update id="updateUser">
        UPDATE sys_user
        SET phone = #{phone}, name = #{name}, trustRate = #{trustRate}
        WHERE id = #{id}
    </update>

    <select id="findById" resultType="com.airent.model.User">
        SELECT *
        FROM sys_user
        WHERE id=#{id}
    </select>

    <select id="findByPhone" resultType="com.airent.model.User">
        SELECT *
        FROM sys_user
        WHERE phone=#{phone}
    </select>

    <update id="updatePassword">
        UPDATE sys_user
        SET password = #{password}
        WHERE id = #{id}
    </update>

    <select id="getUserForAdvert" resultType="com.airent.model.User">
        SELECT *
        FROM sys_user
        WHERE id=(SELECT userId
                  FROM advert_author
                  WHERE advertId=#{advertId}
                  ORDER BY trustRate DESC
                  LIMIT 1)
    </select>

</mapper>